<div class="container">
  <h1><%= User.model_name.human %> <%= t('create') %></h1>

  <%= form_for(@entry, url: admin_users_path, method: :post) do |f| -%>
    <%= render(partial: 'form', locals: { form: f, entry: @entry }) %>
  <% end %>
</div>
