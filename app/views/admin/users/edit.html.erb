<div class="container">
  <h1><%= t('.title') %></h1>

  <%= form_for(@entry, url: admin_user_path(@entry)) do |f| %>
    <%= render(partial: 'form', locals: { form: f, entry: @entry }) %>
  <% end -%>
</div>
